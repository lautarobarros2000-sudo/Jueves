name: Keep Supabase Alive

on:
  schedule:
    - cron: "0 12 */2 * *"
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest

    steps:
      - name: Ping Supabase
        run: |
          STATUS=$(curl -s -o /dev/null -w "%{http_code}" \
          'https://jvefzcnujhpqgyedmmxp.supabase.co/auth/v1/health')

          echo "Status: $STATUS"

          if [ "$STATUS" -ne 200 ]; then
            exit 1
          fi
